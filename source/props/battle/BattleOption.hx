package props.battle;

import flixel.util.FlxSpriteUtil;
import flixel.math.FlxRect;
import flixel.group.FlxSpriteGroup;
import flixel.FlxSprite;

class BattleOption extends FlxSpriteGroup
{
    var rect:FlxSprite;

    public function new(X:Float, Y:Float, RectWidth:Int, RectHeight:Int, Text:String)
    {
        super(X, Y);

        FlxSpriteUtil.drawRect(rect, X,)
    }
}